syntax = "proto3";

option csharp_namespace = "Protos";

service AddressBookLookup {
  rpc GetAddressBook (AddressBookRequest) returns (AddressBook);
  rpc FindPersons (Person) returns (stream Person);
}

message AddressBookRequest {
}

message AddressBook{
	repeated Person persons = 1;
}

message Person {
	string name = 1;
	string surname = 2;
	Address address = 3;
}

message Address {
	string street = 1;
	int32 homeNumber = 2;
	string city = 3;
}